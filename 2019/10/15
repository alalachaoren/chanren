今天继续看昨天的论文，接下来介绍的是两层结构，利用两层结构，粗造层和精细层，来减少冗余和模型更新的计算复杂度和存储的空间，在两层结构中分别设置了三个阈值，
是为了来判断将数据应该与粗糙层进行比对还是对精细层进行比对或者还是先放入缓冲区域或者还是应该删除来进行判断。
在计算阈值方面，文章利用了置信区间来确定阈值，但是在其中的计算中涉及到的隶属度的方面的计算还是不是很懂。
在训练的时候他是将所有标签为正常的数据拿来训练的，然后将剩下的正常的数据以及不正常的数据放在测试集中的，
在后面的实验结果方面，作者提到了用所谓的中心点来代表这个簇然后再将测试的点来计算测试点到簇的距离这种算法比较差，
是因为所谓的中心点并不能完全代表这个簇的数据的分布。这个问题应该引起注意。
文章后面还剩一些实验部分还没有完全看完，明天继续看。
下午在上高级人工智能课。
