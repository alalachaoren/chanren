在后面的实验结果方面，作者提到了用所谓的中心点来代表这个簇然后再将测试的点来计算测试点到簇的距离这种算法比较差，因为所谓的中心点并不能完全
代表这个簇的数据的分布。通过实验证明了忽略冗余，以及批量更新模型不会降低模型检测的准确率。而且会更快，而且降噪，
在这里降噪是放在缓冲区中通过阈值的判断这个簇是新产生的类还是噪声。在文章中每个新进来的簇的大小不一，我们根据进来的簇的大小并且给粗造层和精细层以及缓冲
区分别设置阈值来判断应该放在什么位置来处理这一部分数据，两层结构的高斯模型主要是可以减少冗余数据但是可以不降低数据监测的精度。
下午在上机器学习，
然后在找论文后面的一些参考文献来看看，准备看看能不能找到某种方法能够很好的表示出簇，以及模型的更新方面是否可以相处更好的解决方案。
