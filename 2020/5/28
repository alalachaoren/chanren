今天在看代码，
然后把’UNSUPERVISED CLUSTERING USING PSEUDO-SEMISUPERVISED LEARNING‘往后看了点
他这篇文章大概的思想是是通过神经网络找出无标签数据中相似的类，接下来，通过使用集成模型之间对一对数据点的一致性作为数据点之间相似性的度量。
这种两两配对的数据被用来构造一个相似图，从中提取了k个紧密的数据点簇，作为伪标签。然后进行半监督学习。然后通过迭代再调整模型。
感觉这篇文章还是很有意思。
